/**
 * @file H2
 */

import styled from 'styled-components'
import { font } from 'styled-theme'

const H2 = styled.h2`
  display: flex;
  width: 100%;
  margin-bottom: 10px;
  font-family: ${font('primary')};
  font-size: 16px;
  font-weight: bold;
`

export default H2

